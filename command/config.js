// Optional: HTTPS origin of your separately hosted gateway. Empty uses this host.
// This is public configuration, never put secrets or private app addresses here.
window.COMMAND_GATEWAY_ORIGIN = "";
